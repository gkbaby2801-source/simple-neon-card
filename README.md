Commands to run (terminal)

# create folder, paste files, then:
python -m pip install Pillow
python palette.py   # creates palette.png
# serve the frontend:
# simple way:
python -m http.server 8000
# open http://localhost:8000 in browser and open index.html
